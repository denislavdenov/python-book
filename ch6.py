#!/usr/bin/env python3

import math
type(math)
#help(math)
print(math.sqrt(9))
print(math.pi)
radius = 5 
print('area is', math.pi * radius ** 2)

import temp
celsius = temp.convert_to_celsius(33.1)
print(celsius)
print(temp.above_freezing(celsius))

import experiment

print('__name__ is ',__name__)

import main_example

import temperature_program

import baking